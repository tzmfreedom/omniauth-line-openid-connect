RSpec.describe Omniauth::Line::Openid::Connect do
  it "has a version number" do
    expect(Omniauth::Line::Openid::Connect::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
